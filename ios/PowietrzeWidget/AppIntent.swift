// Intentionally empty. The Widget Extension template generated a configuration
// AppIntent (config-intent option); the Powietrze widget is static (no user config),
// so it's unused. File kept so the Xcode target's source reference resolves.
