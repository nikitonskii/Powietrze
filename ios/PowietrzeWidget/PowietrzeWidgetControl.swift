// Intentionally empty. The Widget Extension template generated a Control-Center
// control (config-intent option); this app ships only the static home-screen widget
// (see PowietrzeWidgetBundle.swift). File kept so the Xcode target's source
// reference resolves; add a ControlWidget here if a Control is wanted later.
